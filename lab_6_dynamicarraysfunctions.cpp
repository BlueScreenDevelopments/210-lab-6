#include <iostream>
#include <iomanip>
using namespace std;

void enterArrayData(double* arr, size_t size);
void outputArrayData(const double* arr, size_t size);
double sumArray(const double* arr, size_t size);